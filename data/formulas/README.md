# Formula JSON corpus

Not included in this public snapshot. Licence for the production corpus has not been confirmed for Apache-2.0 publication.

CKD-EPI 2021 does not need this directory — it is coded in `src/tool_medical_formulas/engine/ckd_epi_creatinine.py`.

To point a private checkout at a local tree:

```bash
set TOOL_MEDICAL_FORMULAS_FORMULAS_DIR=path/to/formulas
```
